import {PeerIo} from '../../src/peer.io';

describe('Hoge', () => {
    beforeEach(() => {
    });

    it('giventaining a greeting and a remove button', () => {
        expect(3).toBe(2);
    });
});

